# Mermaid Sample Project

Same layout as [sample-graphviz](../sample-graphviz), but using Mermaid output for the diagram and markdown for the legend. Check the config in [the root build file](build.gradle.kts).
